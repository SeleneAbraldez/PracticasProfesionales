import { TransformarTextoPipe } from './transformar-texto.pipe';

describe('TransformarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
